# ********RoostGPT********

# Test generated by RoostGPT for test Omni-Collect-Prompt-changed using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /v1/sandbox-self-service/data_post for http method type POST in artillery framework
# RoostTestHash=c4f330440c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "v1_sandbox-self-service_data_post_others.csv"
    skipHeader: true
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
      - "table-name"
      - "product-name"
      - "file-content"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            Authorization: "Bearer invalid_token"
          formData:
            file: "{{ file-content }}"
          expect:
            - statusCode: 401
            - hasHeader: "www-authenticate"
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
          formData:
            file: "{{ file-content }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          body: "This is not a file."
          expect:
            - statusCode: 415
            - hasProperty: "error"
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/invalid-endpoint"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            file: "{{ file-content }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            invalidField: "invalid data"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
