# ********RoostGPT********

# Test generated by RoostGPT for test Omni-Collect-Prompt-changed using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /v1/sandbox-self-service/data_post for http method type POST in artillery framework
# RoostTestHash=c4f330440c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "v1_sandbox-self-service_data_post_others.csv"
    skipHeader: true
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
      - "table-name"
      - "product-name"

scenarios:
  - name: "Non-success API responses Test"
    flow:
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer invalid_token"
          formData:
            file: "testfile.txt"
          expect:
            - statusCode: 401
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            file: "testfile.txt"
          expect:
            - statusCode: 400
            - notHasProperty: "x-hsbc-client-id"

      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          body: "This is a plain text body, not a form-data."
          expect:
            - statusCode: 415

      - think: 1

      - post:
          url: "/v1/sandbox-self-service/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            file: "testfile.txt"
          expect:
            - statusCode: 404

      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            file: "{\"key\":\"value\"}"
          expect:
            - statusCode: 400

      - think: 1
