# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-sandbox using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /v1/sandbox-self-service/data_delete for http method type DELETE in artillery framework
# RoostTestHash=293cb5e3c4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "v1_sandbox-self-service_data_delete_success.csv"
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
      - "table-name"
      - "product-name"
    skipHeader: true

scenarios:
  - name: "Testing successful API responses"
    flow:
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            x-hsbc-profile-id: "{{ profile-id }}"
            product-category: "{{ product-category }}"
            category-version: "{{ category-version }}"
            api-name: "{{ api-name }}"
            api-version: "{{ api-version }}"
            table-name: "{{ table-name }}"
            product-name: "{{ product-name }}"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - contentType: "application/json"
      - think: 1
