# ********RoostGPT********

# Test generated by RoostGPT for test security-scheme-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=ae50169768
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  environments:
    development:
      target: "http://dev.example.com"
    production:
      target: "http://api.example.com"

scenarios:
  - name: "Non-success API Responses"
    flow:
      - post:
          url: "/v2/pet/invalidType"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - post:
          url: "/v2/pet/99999999999"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1

      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "invalidApiKey"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      - post:
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 403
            - hasProperty: "message"
      - think: 1

      - post:
          url: "/v2/invalidUrl/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
