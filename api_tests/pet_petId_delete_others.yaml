# ********RoostGPT********

# Test generated by RoostGPT for test test-swagger-petstore using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=ae50169768
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
  plugins:
    expect: {}
scenarios:
  - name: "Non-Success API Response Test"
    flow:
      # Invalid API key
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "incorrect_api_key"
          expect:
            - statusCode: 401 # Unauthorized
      - think: 1
      # Missing required field in payload
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "Fluffy"
            # Uncomment the line below if 'photoUrls' is required
            # photoUrls: []
          expect:
            - statusCode: 400 # Bad Request
      - think: 1
      # Invalid endpoint
      - get:
          url: "/v2/invalid_endpoint"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Not Found
      - think: 1
      # Test with invalid petId
      - delete:
          url: "/v2/pet/invalid_petId"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Not Found
      - think: 1
