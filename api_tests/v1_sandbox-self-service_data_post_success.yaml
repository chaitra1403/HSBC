# ********RoostGPT********

# Test generated by RoostGPT for test Omni-Collect-Prompt-changed using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /v1/sandbox-self-service/data_post for http method type POST in artillery framework
# RoostTestHash=c4f330440c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "v1_sandbox-self-service_data_post_success.csv"
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "table-name"
      - "product-name"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Upload account or transactions for a clientID"
    flow:
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "{{ client-secret }}"
            x-hsbc-profile-id: "{{ profile-id }}"
            product-category: "{{ product-category }}"
            category-version: "{{ category-version }}"
            api-name: "{{ api-name }}"
            api-version: "{{ api-version }}"
            table-name: "{{ table-name }}"
            product-name: "{{ product-name }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            file: "test/data/file-to-upload.csv"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
            - jmespath: "type(@) == 'object'"
      - think: 1
