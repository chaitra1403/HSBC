# ********RoostGPT********

# Test generated by RoostGPT for test security-scheme-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=ae50169768
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_success.csv"
    fields:
      - "petId"
      - "api_key"
    skipHeader: true

scenarios:
  - name: "Delete Pet Test Scenario"
    flow:
      - log: "Start of the test scenario"
      - delete:
          headers:
            api_key: "{{ api_key }}"
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Pet deleted successfully"
      - think: 1
      - log: "End of the test scenario"
