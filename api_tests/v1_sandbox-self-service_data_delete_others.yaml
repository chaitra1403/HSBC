# ********RoostGPT********

# Test generated by RoostGPT for test Omni-Collect-Prompt-changed using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /v1/sandbox-self-service/data_delete for http method type DELETE in artillery framework
# RoostTestHash=293cb5e3c4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "v1_sandbox-self-service_data_delete_others.csv"
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
      - "table-name"
      - "product-name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      # Invalid client-id in header
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "invalid-client-id"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
          comment: "Expecting 400 Bad Request due to invalid client-id"
      - think: 1

      # Missing authorization header
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
          comment: "Expecting 401 Unauthorized due to missing Authorization header"
      - think: 1

      # Invalid URL
      - post:
          url: "/v1/sandbox-self-service/dta" # Incorrect URL
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
          comment: "Expecting 404 Not Found due to invalid URL"
      - think: 1

      # Unsupported media type
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/xml" # Unsupported Content-Type
          expect:
            - statusCode: 415
            - hasHeader: "content-type"
          comment: "Expecting 415 Unsupported Media Type due to invalid Content-Type header"
      - think: 1

      # Invalid API version in header
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            x-hsbc-client-id: "{{ client-id }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "v0" # Invalid API version
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
          comment: "Expecting 400 Bad Request due to invalid API version"
      - think: 1
