# ********RoostGPT********

# Test generated by RoostGPT for test Omni-Collect-Prompt-changed using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /v1/sandbox-self-service/data_delete for http method type DELETE in artillery framework
# RoostTestHash=293cb5e3c4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "v1_sandbox-self-service_data_delete_others.csv"
    fields:
      - "client-id"
      - "client-secret"
      - "profile-id"
      - "product-category"
      - "category-version"
      - "api-name"
      - "api-version"
      - "table-name"
      - "product-name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - post:
          url: "/invalid-endpoint-path"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-hsbc-client-id: "{{ client-id }}"
            x-hsbc-client-secret: "invalid_secret"
          expect:
            - statusCode: 403
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            invalidField: "someValue"
          expect:
            - statusCode: 422
      - think: 1

      - post:
          url: "/v1/sandbox-self-service/data"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          body: "Unsupported content type"
          expect:
            - statusCode: 415
      - think: 1
